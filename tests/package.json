{
  "name": "jps-fpa-hub-tests",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests for JPS FP&A Hub",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
